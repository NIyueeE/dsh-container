name: Image

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

# 所有 v* tag 构建共用同一个并发组, 串行推送, 避免两个 tag 并发时竞争
# latest; 非 tag 构建仍按 ref 隔离。
concurrency:
  group: image-${{ startsWith(github.ref, 'refs/tags/v') && 'release' || github.ref }}
  cancel-in-progress: false

# 默认只读; 需要写权限的 job 单独声明
permissions:
  contents: read

jobs:
  validate:
    name: Validate scripts and examples
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Shell syntax check
        run: bash -n container/*.sh

      - name: dsh-client-patch is idempotent and warns on drift
        run: |
          set -euo pipefail
          tmp="$(mktemp -d)"
          trap 'rm -rf "$tmp"' EXIT
          mkdir -p \
            "$tmp/dsh/node_modules/@deepseek-ai/dsh-client-connection/lib" \
            "$tmp/dsh/node_modules/@deepseek-ai/dsh-web-frontend/dist"
          cat > "$tmp/dsh/node_modules/@deepseek-ai/dsh-client-connection/lib/client.js" <<'EOF'
          const handle = {
            api,
            isLoopback: pageLocation === void 0 || isLoopbackHostname(pageLocation.hostname),
          };
          EOF
          cat > "$tmp/dsh/node_modules/@deepseek-ai/dsh-web-frontend/dist/index.html" <<'EOF'
          <!doctype html>
          <html>
          <head>
          <script type="module" crossorigin src="/assets/index.js"></script>
          </head>
          <body></body>
          </html>
          EOF
          HOME="$tmp" DSH_CLIENT_PATCH_ROOT="$tmp/dsh" bash container/dsh-client-patch.sh >/dev/null
          HOME="$tmp" DSH_CLIENT_PATCH_ROOT="$tmp/dsh" bash container/dsh-client-patch.sh >/dev/null
          grep -q 'isLoopback: true, // dsh-container remote-proxy patch' \
            "$tmp/dsh/node_modules/@deepseek-ai/dsh-client-connection/lib/client.js"
          if grep -q 'isLoopback: pageLocation === void 0' \
            "$tmp/dsh/node_modules/@deepseek-ai/dsh-client-connection/lib/client.js"; then
            echo "old isLoopback gate still present after patch" >&2
            exit 1
          fi
          test "$(grep -c 'dsh-container randomUUID polyfill' "$tmp/dsh/node_modules/@deepseek-ai/dsh-web-frontend/dist/index.html")" -eq 1
          # Version drift must not fail the supervisor: warn and skip with exit 0.
          cat > "$tmp/dsh/node_modules/@deepseek-ai/dsh-client-connection/lib/client.js" <<'EOF'
          const handle = { api, isLoopback: somethingElse };
          EOF
          cat > "$tmp/dsh/node_modules/@deepseek-ai/dsh-web-frontend/dist/index.html" <<'EOF'
          <html><head><script defer src="/x.js"></script></head></html>
          EOF
          HOME="$tmp" DSH_CLIENT_PATCH_ROOT="$tmp/dsh" bash container/dsh-client-patch.sh >/dev/null 2>&1

      - name: dsh-update must never downgrade a newer installed version
        run: |
          set -euo pipefail
          tmp="$(mktemp -d)"
          trap 'rm -rf "$tmp"' EXIT
          cat > "$tmp/dsh" <<'EOF'
          #!/bin/sh
          echo 99.0.0
          EOF
          cat > "$tmp/npm" <<'EOF'
          #!/bin/sh
          if [ "$1" = "view" ]; then
            echo 1.0.0
            exit 0
          fi
          echo "unexpected npm invocation: $*" >&2
          exit 9
          EOF
          chmod +x "$tmp/dsh" "$tmp/npm"
          PATH="$tmp:$PATH" bash container/dsh-update.sh | grep 'not newer than installed'

      - name: Validate compose example
        run: docker compose -f examples/compose.yaml config --quiet

      - name: Validate quadlet example
        run: |
          sudo mkdir -p /etc/containers/systemd
          sudo cp examples/dsh.container /etc/containers/systemd/
          sudo systemctl daemon-reload
          systemctl cat dsh.service >/dev/null

  image:
    name: Build and validate image
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # GHCR 要求仓库名全小写, 而 github.repository 可能含大写(如 NIyueeE)。
      - name: Compute lowercase image name
        id: image
        run: echo "name=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      # 版本号对齐(仅指镜像自身的版本): 打 v* tag 时, release 版本 ==
      # 镜像 tag(v1.2.0) == OCI 版本 label(1.2.0); 非发布构建 label 为 latest。
      # 镜像版本与 dsh npm 版本解耦: 未传 DSH_VERSION 时构建安装 npm latest,
      # 运行时默认不自动更新, 版本由镜像 tag 决定。
      - name: Compute version metadata
        id: version
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "label=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "label=latest" >> "$GITHUB_OUTPUT"
          fi

      # 组装 build args: DSH_VERSION 仅当仓库变量(Settings → Variables → DSH_VERSION)
      # 设置了才传, 避免空值覆盖 Containerfile 里的默认值。
      - name: Compute build args
        id: args
        run: |
          {
            echo "BUILD_GIT_SHA=${{ github.sha }}"
            echo "BUILD_GIT_REF=${{ github.ref_name }}"
            echo "BUILD_VERSION=$label"
            [ -z "$dsh" ] || echo "DSH_VERSION=$dsh"
          } > /tmp/build-args.txt
          {
            echo "args<<EOF"
            cat /tmp/build-args.txt
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
        env:
          label: ${{ steps.version.outputs.label }}
          dsh: ${{ vars.DSH_VERSION }}

      # 只有 v* tag(正式发布)才推送镜像; 其余事件(main / PR / dispatch)
      # 只构建 + 冒烟验证临时镜像, 不产生任何 GHCR 产物。
      # 单平台(当前基础镜像按 amd64 验证), load 后本地可直接冒烟。
      - name: Build image (validation)
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Containerfile
          push: false
          load: true
          tags: |
            ${{ steps.image.outputs.name }}:${{ github.sha }}
            ${{ startsWith(github.ref, 'refs/tags/v') && format('{0}:{1}', steps.image.outputs.name, github.ref_name) || '' }}
            ${{ startsWith(github.ref, 'refs/tags/v') && format('{0}:latest', steps.image.outputs.name) || '' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: ${{ steps.args.outputs.args }}

      - name: Smoke test
        run: |
          set -euo pipefail
          image="${{ steps.image.outputs.name }}:${{ github.sha }}"
          # 桥接 + 端口映射, 与默认部署方式一致(对外端口 3081)。
          cid="$(docker run -d --rm --name dsh-smoke -p 3081:3081 \
            -e DSH_HOME=/tmp/dsh-smoke-home \
            -e DSH_AUTO_UPDATE=0 \
            "$image")"
          trap 'docker rm -f "$cid" >/dev/null 2>&1 || true' EXIT

          # 等待 Web UI 就绪(首次启动需自动初始化 web profile)
          for _ in $(seq 1 60); do
            if curl -fsS -o /dev/null http://127.0.0.1:3081/ 2>/dev/null; then
              break
            fi
            sleep 2
          done
          # grep 不带 -q: -q 匹配即退出会让管道上游(docker exec/curl)写
          # 入已关闭的管道触发 SIGPIPE(141), 在 set -euo pipefail 下误报失败。
          if ! curl -fsS http://127.0.0.1:3081/ | grep '<title>DeepSeek Harness</title>' >/dev/null; then
            echo "=== dsh-smoke container logs ===" >&2
            docker logs "$cid" >&2 2>&1 || true
            exit 1
          fi

          # 工具链与 dsh 版本
          docker exec "$cid" dsh --version | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+'
          docker exec "$cid" node --version | grep -Eq '^v[0-9]+\.'
          docker exec "$cid" cargo --version | grep -Eq '^cargo [0-9]+\.'
          docker exec "$cid" uv --version | grep -Eq '^uv [0-9]+\.'
          docker exec "$cid" pnpm --version | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+'
          docker exec "$cid" podman --version | grep -Eq '^podman version [0-9]+\.'
          docker exec "$cid" gh --version | grep -Eq '^gh version [0-9]+\.'
          # 用户级工具目录必须可写/可执行, 并位于持久化的 /home/dsh 下。
          docker exec "$cid" sh -c '
            home="${HOME:-$(getent passwd "$(id -u)" | cut -d: -f6)}"
            rustup_home="${RUSTUP_HOME:-$home/.rustup}"
            cargo_home="${CARGO_HOME:-$home/.cargo}"
            pnpm_home="${PNPM_HOME:-$home/.local/share/pnpm}"
            test -w "$rustup_home"
            test -w "$cargo_home"
            test -x "$home/.local/bin/uv"
            test -x "$home/.local/bin/pnpm"
            mkdir -p "$cargo_home/registry/cache" "$cargo_home/registry/index" "$pnpm_home"
          '
          # Caddy 代理已在容器内运行(头改写链路)
          docker exec "$cid" sh -c 'pgrep -x caddy >/dev/null'

          # 头改写代理链路: settings.describe 属上游仅回环特权方法, 经代理
          # 改写 Host/Origin 后应返回 200(远程访问全功能的核心保证)。
          settings_code="$(curl -s -o /dev/null -w '%{http_code}' -X POST \
            http://127.0.0.1:3081/api/settings.describe -H 'content-type: application/json' \
            --data '{"type":"client-request","rpcId":"1","method":"settings.describe","payload":{}}')"
          [ "$settings_code" = "200" ]

          # 前端兼容补丁: 代理返回的 HTML 必须带 crypto.randomUUID polyfill,
          # 返回的 connection client bundle 必须把 isLoopback 全局放开, 且旧的
          # 非回环判断必须消失。再用非回环 Host 头模拟远程浏览器请求代理。
          curl -fsS http://127.0.0.1:3081/ | grep -q 'dsh-container randomUUID polyfill'
          curl -fsS http://127.0.0.1:3081/plugins/@deepseek-ai/dsh-client-connection/client.js | \
            grep -q 'isLoopback: true, // dsh-container remote-proxy patch'
          if curl -fsS http://127.0.0.1:3081/plugins/@deepseek-ai/dsh-client-connection/client.js | \
            grep -q 'isLoopback: pageLocation === void 0'; then
            echo "old isLoopback gate is still present in the served client bundle" >&2
            exit 1
          fi
          curl -fsS -H 'Host: dsh.test' http://127.0.0.1:3081/ | grep -q 'dsh-container randomUUID polyfill'
          curl -fsS -H 'Host: dsh.test' http://127.0.0.1:3081/plugins/@deepseek-ai/dsh-client-connection/client.js | \
            grep -q 'isLoopback: true, // dsh-container remote-proxy patch'
          docker exec "$cid" sh -c 'command -v dsh-client-patch >/dev/null'

          # 真实更新链路: 卸载后由 dsh-update 重新安装, 验证 uid 1000 的
          # 全局 npm install(含 node-pty 等原生模块 install scripts)全链路可用。
          docker exec "$cid" npm uninstall --global --no-fund --no-audit @deepseek-ai/dsh
          docker exec "$cid" dsh-update | grep -E 'installing|installed|updating|already up to date'
          docker exec "$cid" dsh --version | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+'
          # 幂等: 再次运行不再安装
          docker exec "$cid" dsh-update | grep 'already up to date'

          # dsh web 守护/重启: dsh-restart 后 dsh web 仍可用
          docker exec "$cid" dsh-restart
          for _ in $(seq 1 60); do
            if curl -fsS -o /dev/null http://127.0.0.1:3081/ 2>/dev/null; then
              break
            fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:3081/ | grep '<title>DeepSeek Harness</title>' >/dev/null
          # 更新 + 重启后补丁必须仍然生效(dsh-web 会在每次启动前重打补丁),
          # 旧的非回环判断也必须仍然不存在。
          curl -fsS http://127.0.0.1:3081/ | grep -q 'dsh-container randomUUID polyfill'
          curl -fsS http://127.0.0.1:3081/plugins/@deepseek-ai/dsh-client-connection/client.js | \
            grep -q 'isLoopback: true, // dsh-container remote-proxy patch'
          if curl -fsS http://127.0.0.1:3081/plugins/@deepseek-ai/dsh-client-connection/client.js | \
            grep -q 'isLoopback: pageLocation === void 0'; then
            echo "old isLoopback gate reappeared after update/restart" >&2
            exit 1
          fi

          # basic auth 模式: 未带凭据 401, 带凭据 200。
          auth_cid="$(docker run -d --rm --name dsh-smoke-auth -p 127.0.0.1:3082:3081 \
            -e DSH_HOME=/tmp/dsh-smoke-auth-home \
            -e DSH_AUTO_UPDATE=0 \
            -e DSH_PROXY_USER=smoke \
            -e DSH_PROXY_PASSWORD=smoke-pass \
            "$image")"
          trap 'docker rm -f "$cid" "$auth_cid" >/dev/null 2>&1 || true' EXIT
          for _ in $(seq 1 60); do
            if curl -s -o /dev/null http://127.0.0.1:3082/ 2>/dev/null; then
              break
            fi
            sleep 2
          done
          unauth="$(curl -s -o /dev/null -w '%{http_code}' -X POST \
            http://127.0.0.1:3082/api/host.describe -H 'content-type: application/json' \
            --data '{"type":"client-request","rpcId":"1","method":"host.describe","payload":{}}')"
          auth="$(curl -s -o /dev/null -w '%{http_code}' -u smoke:smoke-pass -X POST \
            http://127.0.0.1:3082/api/host.describe -H 'content-type: application/json' \
            --data '{"type":"client-request","rpcId":"1","method":"host.describe","payload":{}}')"
          [ "$unauth" = "401" ] && [ "$auth" = "200" ]

          # 只设置 basic auth 一半变量时必须 fail closed, 不能静默裸奔。
          if timeout 30 docker run --rm --name dsh-smoke-partial-auth \
              -e DSH_HOME=/tmp/dsh-smoke-partial-home \
              -e DSH_AUTO_UPDATE=0 \
              -e DSH_PROXY_USER=smoke \
              "$image" >/tmp/dsh-partial-auth.log 2>&1; then
            echo "container with only DSH_PROXY_USER set unexpectedly started" >&2
            cat /tmp/dsh-partial-auth.log >&2 || true
            exit 1
          fi
          grep -F 'must be set together' /tmp/dsh-partial-auth.log >/dev/null

      # 冒烟通过后才推送(正式发布): v* 版本 tag + sha + latest(指向最近一次发布,
      # 供示例直接拉取)。坏镜像不会进入 GHCR。
      - name: Push image (release only)
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          set -euo pipefail
          image="${{ steps.image.outputs.name }}"
          docker push "$image:${{ github.sha }}"
          docker push "$image:${{ github.ref_name }}"
          docker push "$image:latest"

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: image
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      # 镜像构建 + 冒烟通过后才创建 Release 页面;
      # 版本号与镜像 tag 对齐(均由 git tag 派生, 如 v1.2.0)。
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          make_latest: true
